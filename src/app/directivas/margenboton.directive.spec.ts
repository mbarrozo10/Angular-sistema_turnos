import { MargenbotonDirective } from './margenboton.directive';

describe('MargenbotonDirective', () => {
  it('should create an instance', () => {
    const directive = new MargenbotonDirective();
    expect(directive).toBeTruthy();
  });
});
