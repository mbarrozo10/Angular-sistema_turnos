import { FilaDirective } from './fila.directive';

describe('FilaDirective', () => {
  it('should create an instance', () => {
    const directive = new FilaDirective();
    expect(directive).toBeTruthy();
  });
});
